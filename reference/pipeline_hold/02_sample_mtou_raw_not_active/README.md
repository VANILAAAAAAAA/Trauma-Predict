# 02_sample_mtou_raw_not_active

This folder is parked reference only.

The code here was moved out of active `sample builder/single/` because it is **raw MIMIC-IV row extraction**, not a complete model task sample builder.

Use it only as implementation reference after upstream contracts are fixed:

1. field adapter registry
2. input schema
3. summary/report schema
4. final single-sample contract

Do not promote this code into active `sample builder/` without updating the full sample-builder contract.
